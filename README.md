# DOA Solutions Website

WordPress website files for DOA Solutions.

This repository intentionally does not include local/private runtime files such as `wp-config.php`, `.private/`, uploads, cache, backup folders, or temporary upgrade data. Keep the production server's own `wp-config.php` and media uploads in place when pulling changes.
